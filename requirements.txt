# Core ML/DL (Updated for Python 3.12 + Google Colab compatibility)
# NOTE: For Google Colab, use the notebook's runtime-aware installation instead

# NumPy: MUST be <2.0 for binary compatibility with PyTorch pre-compiled wheels
numpy>=1.26.4,<2.0

# PyTorch: Runtime-detected in Colab (typically 2.x)
torch>=2.0.0
torchvision>=0.15.0

# Transformers: Locked with compatible tokenizers for Colab training
transformers==4.35.0            # Tested and stable
tokenizers>=0.14.0,<0.15        # Compatible with transformers 4.35.0
accelerate>=0.24.0              # For mixed precision training

# PyTorch Geometric: Installed via runtime-aware wheels in Colab notebook
torch-geometric==2.4.0
# Note: torch-scatter, torch-sparse, torch-cluster, torch-spline-conv
# are installed with version-specific wheel URLs in the notebook

# Optional: Only for local development (causes conflicts in Colab)
# sentence-transformers>=2.7.0  # Commented out - conflicts with transformers 4.35.0

# Explainability (NEW)
captum>=0.7.0  # Integrated Gradients
shap>=0.45.0
lime>=0.2.0.1

# Code Analysis
tree-sitter==0.20.4              # Tested and stable for Colab
# tree-sitter-c>=0.24.0          # Cloned from GitHub in notebook
# tree-sitter-python>=0.21.0
# tree-sitter-javascript>=0.21.0

# Graph Database (NEW)
neo4j>=5.19.0
py2neo>=2021.2.4

# Local Agent (NEW)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# Symbolic Execution (NEW) - Note: angr may have Python 3.12 compatibility issues
# angr>=9.2.100; python_version < '3.12'  # Commented out for Python 3.12
z3-solver>=4.13.0

# Data & Processing
pandas>=2.2.0
# numpy>=1.26.4,<2.0  # Already specified above with ML deps
scipy==1.11.4                    # Tested with Colab
scikit-learn==1.3.2              # Tested with Colab
faiss-cpu>=1.8.0
tqdm                             # Progress bars

# API & Serialization
pydantic>=2.7.0
python-jsonrpc-server>=0.4.0

# Development
pytest>=8.1.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
black>=24.3.0
pylint>=3.1.0
mypy>=1.9.0
isort>=5.13.2

# AWS
boto3>=1.34.0
sagemaker>=2.214.0

# Utilities
python-dotenv>=1.0.1
rich>=13.7.1
click>=8.1.7
watchdog>=4.0.0  # File monitoring

# Additional dependencies for Git operations
GitPython>=3.1.43
requests>=2.31.0
